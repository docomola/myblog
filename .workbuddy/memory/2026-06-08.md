# 2026-06-08 工作日志

## 构建预览
- 修复 TypeScript 类型错误（index.astro: 未使用变量 idx、EventTarget 类型问题）
- 成功构建 71 个静态页面，启动预览服务器（端口 4321）

## 首页居中修复
- 修复 `.intro-headline` 的 `text-align: left` → `text-align: center`
- 解决 "Where Design Meets Technology & Ideas" 模块未居中的问题

## 移动端按钮流光效果修复
- 给 `.glow-btn-wrap` 添加 `-webkit-mask-image` 和 `transform: translateZ(0)`，修复 WebKit 浏览器中 `overflow: hidden` + `border-radius` 无法正确裁剪伪元素的问题
- 在移动端媒体查询中，设置 `.glow-btn-wrap` 和 `.glow-btn` 为 `width: 100%`，使流光按钮与其他按钮宽度一致
- 将 `-webkit-mask-image` / `mask-image` 替换为 `-webkit-clip-path: inset(0 round 9999px)` / `clip-path: inset(0 round 9999px)`，修复 mask 遮罩导致桌面端流光效果被裁剪的问题
