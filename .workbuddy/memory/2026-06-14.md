# 2026-06-14 工作日志
## 移除全局极光效果
- **文件**: `src/layouts/BaseLayout.astro`
- 移除 `.page-aurora` CSS（fixed 全屏背景）
- 移除 `<div id="page-aurora">` DOM 元素
- 移除 aurora.js 的 import、initAurora/destroyAurora 调用及 astro:before-swap/after-swap 事件监听
- aurora.js 脚本文件保留，后续如需可重新引入

## Logo 墙重构：CSS 无限滚动（仿 nabhan-astro 风格）
- **文件**: `src/pages/index.astro`
- HTML: 每个 logo 项内双 `<img>` 实现 CSS `translateX(-50%)` 无缝循环
- CSS: 纯 CSS `@keyframes brand-scroll` 动画替代 JS RAF 方案
- 渐变遮罩: `mask-image: linear-gradient()` 替代 `::before/::after` 伪元素
- 悬停: 暂停动画 + logo 亮度提升
- 参数: 速度 40s, 间距 60px, 高度 40px（移动端适配）
- 移除 ~60 行 JS LogoLoop 代码

## Hero 添加条纹纹理背景
- **文件**: `src/pages/index.astro`
- `.hero-card` 背景: 双层 `repeating-linear-gradient` 对角线细线
  - Layer 1: 28°, 14px 间距, 1px 线宽
  - Layer 2: -32°, 22px 间距, 1px 线宽
- 替代原来 `background: transparent`，ColorBends 层覆盖其上

## 博客 C 模块添加条纹背景（已撤回）并重构为 Showcase Carousel（已撤回并恢复原样）
- 博客页面已完全恢复原来的 `.blog-card-list` 垂直列表
- 轮播组件已移至首页

## 首页新增 Showcase Carousel 模块
- **文件**: `src/pages/index.astro`
- 位置：Brand Marquee 之后、Hero Block 之前
- 数据源：`latestFourPosts`（最新4篇文章）
- 标题：FEATURED / 精选文章
- 结构同参考截图：编号 01-04、左右分栏、进度条、圆点导航、←/→箭头
- JS：点击圆点/箭头切换 active slide
